export const environment = {
  production: false,
  // The backend server's address. Change this if you deploy the backend
  // somewhere other than your own machine (e.g. https://api.mystore.com/api).
  apiUrl: 'http://localhost:4000/api',
};
